# Session 2026-04-24

- Target upstream release: `CLIProxyAPI v6.9.36` (`f1ba6151a99240902bcda12102c921b0ead01d2d`)
- Base local release line before merge: `ProxyPilot main` at upstream `v6.9.30` (`2ebbb596`)
- Merge branch: `sync-upstream-v6.9.30-to-v6.9.36`
- Conflict resolution:
  - no textual conflicts in the upstream merge; existing ProxyPilot fork surfaces carried forward cleanly
  - corrected stale upstream marker files from the prior `v6.9.30` sync so `.upstream-sync-tag` and `.upstream-sync-sha` now match the merged upstream release
  - kept the existing fork behavior around local provider support while taking upstream Codex image-generation, Antigravity credits fallback, logging, and `/healthz` improvements
- Validation:
  - `go test ./...`
  - `go build ./cmd/server ./cmd/proxypilot-tray ./cmd/proxypilotpack`
  - `go vet ./...`
